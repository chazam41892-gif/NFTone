# NFTones — Hand-off (2026-05-14)

**Status:** Stack B (Next.js product) code-complete for v1 purchase pipeline. Cannot be runtime-verified in this session — `registry.npmjs.org` and `pypi.org` are unreachable from this sandbox. Watermarker (Python service) verified working: `27 passed, 1 xfailed` against existing venv.

This file documents what was done, what blocks remain, and the exact commands you need to run on your machine to close every gap.

## What this session changed

### New files

| File | Purpose |
|---|---|
| `lib/watermarker.ts` | HTTP client for `services/audio_watermarker`. Exports `embedWatermark`, `detectWatermark`, `watermarkerHealth`. |
| `_archive/2026-05-14/app/api/artists/route.ts` | Archived (was orphan + accepted `userId` in body without auth). |
| `_archive/2026-05-14/app/api/artists/README.md` | Why this route was archived (security hole + superseded by `/api/onboard`). |
| `HANDOFF-2026-05-14.md` | This file. |

### Modified files

| File | What changed | Why |
|---|---|---|
| `prisma/schema.prisma` | Added `Drop.masterSha256`, enum `PurchaseStatus`, fields `Purchase.status \| derivativeSha256 \| watermarkedAudioUrl \| walletFingerprint \| failureReason \| updatedAt`, indexes on `Purchase.dropId/buyerWallet/status`. | Watermark records + per-buyer derivative tracking + mint lifecycle. |
| `lib/storage.ts` | Added `downloadAudio()` and `uploadWatermarkedAudio()`. | Purchase route needs to fetch master, deliver buyer-specific watermarked copy to Supabase. |
| `lib/solanaMint.ts` | Replaced placeholder that returned hardcoded fake addresses with an honestly-gated discriminated-union result: `{status: "minted"\|"pending"\|"failed"}`. | The previous version returned `"DEVNET_PLACEHOLDER_NFT_ADDRESS"` as if it had minted. That was a lie — fixed. Now: if `PLATFORM_KEYPAIR_BASE58` is unset, returns `pending` with a clear reason; the Metaplex implementation is commented in with exact install/uncomment steps. |
| `app/api/drops/[id]/purchase/route.ts` | End-to-end pipeline: fetch master → call watermarker → upload watermarked → record `Purchase` → try mint → set status accordingly. Each failure path records `Purchase.failureReason`. | The old route just minted a fake address. This is the real flow. |
| `app/api/ktrs/credits/route.ts` | Reads `userId` from `auth()` session, not body. `action: "add"` is now refused (returns 403) — credit grants must come from Stripe webhooks / admin / on-chain proof. | Security hole: any caller could add credits to any user. |
| `.env.example` | Added `NFTONES_WATERMARKER_URL` and `PLATFORM_KEYPAIR_BASE58` with comments explaining what each gates. | New deps require new env. |
| `tsconfig.json` | Added `_archive` and `lvtn-adapter-reference` to `exclude`. | Don't compile archived dead code or a reference adapter. |

### What was NOT changed (and why)

- **Metaplex on-chain mint** — code path is commented in `lib/solanaMint.ts` with exact `npm i` for the three Metaplex packages. Not wired live because (a) requires `PLATFORM_KEYPAIR_BASE58` you haven't generated, (b) requires funded devnet SOL, (c) adds three deps that need network to install.
- **Stripe / on-chain payment processor** — the purchase route still treats the request as already-paid. Note marked `PRODUCTION TODO — payment` in [app/api/drops/[id]/purchase/route.ts:42-49](app/api/drops/%5Bid%5D/purchase/route.ts#L42-L49).
- **Tier enforcement on purchase quantity (edition size)** — `editionSize` exists on the model but isn't decremented per sale yet. Add when you add real payment.
- **Audio chunk uploads** — `app/api/upload/route.ts` body-caps at 50MB and notes that Vercel serverless has a 4.5MB cap. For prod, switch to signed-URL direct-to-Supabase uploads.

## Cross-stack verification — what actually ran in this session

| Check | Result | How |
|---|---|---|
| `services/audio_watermarker` pytest | ✅ **27 passed, 1 xfailed in 10.67s** | `.venv/Scripts/python.exe -m pytest -q` (venv pre-existed, no install needed) |
| All `@/lib/*` and `@/components/*` imports point at files that exist | ✅ | Grep-verified — see `Grep` block at end. |
| No remaining references to `/api/artists` after archive | ✅ | `grep -r "api/artists"` returns only the archive README. |
| `prisma/schema.prisma` references in code match new fields | ✅ | Purchase route reads/writes `status`, `derivativeSha256`, `watermarkedAudioUrl`, `walletFingerprint`, `failureReason`. All exist in schema. |

## What remains BLOCKED — exact commands to unblock

### 1. Install Stack B (one-time, ~3 min)

```powershell
cd C:\Users\chaza\NFTones
npm install
```

This is the gate to everything below. The sandbox couldn't reach `registry.npmjs.org`; your box should.

### 2. Validate Prisma schema + generate client

```powershell
npx prisma validate
npx prisma generate
```

Expected: no errors. If `prisma validate` complains about `PurchaseStatus` enum, the schema is fine — that error usually means an old generated client; just re-run generate.

### 3. Typecheck the whole tree

```powershell
npx tsc --noEmit
```

Expected: zero errors. I scanned imports by hand but `tsc` is authoritative. If something breaks, the most likely culprits are (a) `next-auth` v5-beta type drift, (b) a `(session.user as any).id` cast that's no longer needed once `types/next-auth.d.ts` is picked up.

### 4. Build the Next.js app

```powershell
npx next build
```

Expected: build succeeds. If it complains about server-only modules in client components, those would surface here.

### 5. Migrate the database

Pick one path:

**Path A — local Postgres:**
```powershell
# Start a local postgres (Docker is fine):
docker run -d --name nftones-pg -e POSTGRES_PASSWORD=dev -p 5432:5432 postgres:16
# Update .env (NOT .env.example):
#   DATABASE_URL="postgresql://postgres:dev@localhost:5432/postgres"
npx prisma migrate dev --name init
```

**Path B — Supabase Postgres (the intended host, `mzwvfkvxxnmipjilzlyr`):**
```powershell
# Get the connection string from Supabase Studio → Project Settings → Database
# Put it in .env as DATABASE_URL=
npx prisma migrate dev --name init
```

The MCP server in `.mcp.json` is wired but **not authenticated** in this sandbox. Authenticate via `mcp__supabase__authenticate` next session, then use `mcp__supabase__apply_migration` to apply migrations directly to that project.

### 6. Start the watermarker service

```powershell
cd C:\Users\chaza\NFTones\services\audio_watermarker
# venv already exists with deps; if not, see services/audio_watermarker/README.md
.\.venv\Scripts\activate
uvicorn src.api:app --port 8500

# In another shell, prove it:
curl http://localhost:8500/api/v1/health
# expect: {"status":"ok","service":"nftones-audio-watermarker"}
```

### 7. Boot Next.js + manual smoke

```powershell
cd C:\Users\chaza\NFTones
npm run dev
```

Then in the browser:
1. `http://localhost:3000` — landing renders
2. `http://localhost:3000/signin` — Phantom wallet connects, message-signing flow works (needs the wallet extension installed)
3. After signin, `/onboard` — create artist profile with a slug
4. `/dashboard` — upload an audio file, fill rights declaration, hit "Create Lazy Mint Drop"
5. `/drop/<id>` — see the drop; another wallet hits "Buy / Mint When Sold" and the purchase row gets created with `status = WATERMARKED` (since `PLATFORM_KEYPAIR_BASE58` isn't set yet)
6. Detect-test: hand the watermarked URL back to the watermarker's `/api/v1/watermark/detect` and confirm it returns the buyer's wallet.

## User-only blockers (things only YOU can supply)

| Blocker | What I need | Why |
|---|---|---|
| **`PLATFORM_KEYPAIR_BASE58`** | Generate with `solana-keygen new`, base58-encode the secret. | Required for real on-chain mint in `lib/solanaMint.ts`. Without it, purchases complete + watermark + DB write, but mint stays pending. |
| **Metaplex packages** | `npm i @metaplex-foundation/umi @metaplex-foundation/umi-bundle-defaults @metaplex-foundation/mpl-token-metadata` then uncomment the `REAL MINT` block in `lib/solanaMint.ts`. | Three packages I didn't add to `package.json` because I couldn't verify they install in this sandbox. |
| **Funded devnet SOL** | `solana airdrop 2 <platform-wallet>` on devnet. | Mint instructions need rent + fees. |
| **Supabase project access** | Either authenticate the MCP next session, or paste `DATABASE_URL` + `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` into `.env`. | Needed to run migrations and to upload audio. |
| **Supabase Storage bucket** | Create a public bucket named `nftones` in the Supabase dashboard. | `lib/storage.ts` writes to `BUCKET = "nftones"`. |
| **`AUTH_SECRET`** | `openssl rand -base64 32` and paste into `.env`. | Required for NextAuth v5 JWT signing. |
| **Code-signing certs** (Phase 8) | Authenticode cert from a CA. | Required to sign the Tauri/Bubblewrap installers before store submission. |
| **Store accounts** (Phase 8) | Apple Dev, Google Play Console, MS Partner Center. | Required to submit the mobile/desktop builds. |
| **Lawyer-reviewed privacy policy + ToS** | Plain English + legal review. | Required by Apple/Google before store approval. |

## What still doesn't exist (deliberate scope cuts — separate sessions)

These were NOT done because they're separate concerns and the user said "complete" in the context of the production-shippable web stack, not the full Phase 3-8 roadmap:

- **Phase 3** — Tauri desktop shell. The web stack here ships as-is; wrapping it in Tauri is a discrete next step (~1 day).
- **Phase 4** — Cloudflare Pages deploy + Bubblewrap AAB. No CI yet.
- **Phase 6** — Watermark detection UI. The detect endpoint exists in the Python service; no Next.js route exposes it to users yet. (Easy add: `app/api/scans/route.ts` → POST audio → forward to watermarker `/detect`.)
- **Phase 7** — Anchor program on devnet. Lazy minting via Metaplex (above) is the path; a custom Anchor program is only needed if you want platform-specific on-chain logic (e.g., royalty enforcement contracts).
- **Phase 8** — Alembic for Stack A. Prisma migrations replace this for Stack B, which is where the data lives going forward.

## Anti-regression list (do NOT do these)

- **Do NOT** put the `userId`-in-body pattern back into any route. The `/api/artists` archive exists precisely so future-you can read the README and remember why.
- **Do NOT** have `mintNftOnPurchase` return a fake `DEVNET_PLACEHOLDER` address again. The new type is a discriminated union for a reason — `status: "pending"` is honest; "we minted but here's a fake string" is a lie.
- **Do NOT** delete files in Stack A (`web/`, `services/nftones-api/`) until Stack B has parity AND you've confirmed nothing in `lvtn-adapter-reference/` depends on them.
- **Do NOT** skip `npx prisma generate` before `npx tsc --noEmit` — the type errors that show up otherwise are misleading.

## Sandbox-specific blockers I hit (FYI, not your problem)

| Tool | Symptom | Workaround |
|---|---|---|
| `npm install` | `curl https://registry.npmjs.org` returns 000. Install hangs indefinitely. | Run on your box; the sandbox can't reach it. |
| `pip install` | `pypi.org` SSL handshake errors. | Run on your box (Stack A pytest must run there). |
| `mcp__supabase__*` | Not authenticated in sandbox. | Run `mcp__supabase__authenticate` next session. |

## Verification log — what I actually ran in this session

```text
$ cd services/audio_watermarker
$ .venv/Scripts/python.exe -m pytest -q
...........................x                                             [100%]
27 passed, 1 xfailed in 10.67s

$ grep -r "api/artists" .
./\_archive/2026-05-14/app/api/artists/README.md:1:# Archived: app/api/artists/route.ts
./\_archive/2026-05-14/app/api/artists/README.md:14:No frontend code referenced `/api/artists` (verified via grep before move).

$ curl --max-time 5 https://registry.npmjs.org
# returns 000 (no connectivity)

$ pip install -r requirements-dev.txt
# pypi.org: SSL UNEXPECTED_MESSAGE / Read timed out

$ node --version
v24.13.0
$ npm --version
11.6.2
```

That's the truth of this session: code is complete and consistent; one of the four real-world tests (watermarker) ran; the other three (`tsc`, `next build`, `pytest` on `nftones-api`) need your machine.
